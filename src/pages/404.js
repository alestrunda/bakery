import React from 'react'

import Layout from '../layouts/Page'

const NotFoundPage = () => (
  <Layout>
    <h1 className="heading-main-single">NOT FOUND</h1>
    <p>You just hit a route that doesn&#39;t exist... the sadness.</p>
  </Layout>
)

export default NotFoundPage
